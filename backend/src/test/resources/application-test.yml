spring:
  datasource:
    url: jdbc:h2:mem:sre_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

app:
  environment: TEST
  version: test
  cors:
    allowed-origins: '*'
